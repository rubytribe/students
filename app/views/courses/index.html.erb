<h2> Courses </h2>

<%= link_to 'Add Course', new_course_path %>

<ul>
<%= will_paginate %>
  <table>
    <tr>
      <th> Course name </th>
    </tr>
  <%= render @courses %>
<%= will_paginate %>
</ul>
